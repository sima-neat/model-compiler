id: sima-model-surgery
name: General ONNX Model Surgery for SiMa MLA
version: 0.0.2
agents: [codex, claude]
description: Fix SiMa MLA operator incompatibilities or adapt model outputs for SiMa BoxDecode.
compatibility:
  env_types: [host, sdk]
  env_subtypes: [linux, palette]
  min_cli_version: 2.1.2
runtime:
  requires:
    - python3
