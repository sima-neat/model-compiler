---
title: Model compatibility
sidebar_position: 2
---

<!-- AUTO-GENERATED by scripts/build_supported_operators_md.py. Do not edit by hand. -->

# Model compatibility

The SiMa.ai compilation toolchain imports **ONNX** models and prepares operators for the Machine Learning Accelerator (MLA). Use this page to check operator support before you prepare a model for Modalix.

## Supported operators

Search the table to check MLA compiler support by operator and precision scheme. **INT8** runs on the MLA. **BF16** is available on Modalix (developer preview). **5D** marks operators that accept 5D (N, D, H, W, C) tensors. **Opset** is the ONNX opset version. Search matches constraint text as well as operator names, and each operator links to its reference on onnx.ai.

<OperatorTable>
[{"name":"Abs","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"Add","int8":"✅","bf16":"✅","fived":"—","opset":"14","constraint":"Inputs must have the same shape, one input must be a scalar, or the two must be broadcastable."},{"name":"ArgMax","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"Reduces along the channel axis only, and returns `int32` rather than the `int64` of the ONNX spec. Cast downstream consumers accordingly."},{"name":"AveragePool","int8":"✅","bf16":"✅","fived":"—","opset":"11","constraint":"Set `count_include_pad=True` and leave `dilations` at 1. Kernel size must be under 128 in each spatial dimension; global pooling has no size limit."},{"name":"BatchNormalization","int8":"—","bf16":"—","fived":"—","opset":"15","constraint":"Export in inference mode. Training mode is not supported."},{"name":"Clip","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"Concat","int8":"✅","bf16":"✅","fived":"✅","opset":"13","constraint":"Concatenation along the batch axis is not supported."},{"name":"Conv","int8":"✅","bf16":"✅","fived":"✅","opset":"11","constraint":"`dilations` must be between 1 and 63, and `strides` between 1 and 31."},{"name":"ConvTranspose","int8":"✅","bf16":"✅","fived":"✅","opset":"11","constraint":"Leave `dilations` at 1. The operator must either be depthwise or use `group=1`. Stride must be 1, 2, 4, 8, or 16, or 1 or 2 when depthwise."},{"name":"Cos","int8":"✅","bf16":"❌","fived":"✅","opset":"22","constraint":"Runs as an INT8 lookup-table UDF. In a BF16 model, AFE inserts casts and executes `Cos` in INT8."},{"name":"CumSum","int8":"—","bf16":"—","fived":"—","opset":"14","constraint":"Sums in the forward direction only, along exactly one axis, and that axis must be a constant. The axis can be at most 257 elements long."},{"name":"DepthToSpace","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"Div","int8":"✅","bf16":"✅","fived":"—","opset":"14","constraint":"Inputs must have the same shape, one input must be a scalar, or the two must be broadcastable."},{"name":"Einsum","int8":"✅","bf16":"✅","fived":"—","opset":"12","constraint":"Takes exactly two input tensors. Every index in the equation must appear in at least two of its terms, and no more than once within any single term. For example, `jk,k->k` is unsupported because `j` appears in only one term, and `jk,kk->j` is unsupported because `k` appears twice in the same term."},{"name":"Elu","int8":"✅","bf16":"✅","fived":"—","opset":"6","constraint":""},{"name":"Erf","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"Exp","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"Expand","int8":"✅","bf16":"✅","fived":"✅","opset":"13","constraint":"The `shape` input must be a compile-time constant and broadcast-compatible with the input. The batch dimension cannot be broadcast or changed."},{"name":"Flatten","int8":"✅","bf16":"✅","fived":"—","opset":"21","constraint":"Use `axis=1`. Flattening must preserve the batch dimension, and both input and output must be at least 2D."},{"name":"Gather","int8":"❌","bf16":"✅","fived":"—","opset":"13","constraint":"`indices` must be a constant, and either a scalar or a 1D tensor."},{"name":"Gelu","int8":"✅","bf16":"❌","fived":"—","opset":"20","constraint":"Only the exact formulation is supported. Set `approximate=\"none\"`."},{"name":"Gemm","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"`A` and `B` must be rank 2, and `B` must be a constant initializer. `transA=1` requires flexible batching to be disabled. Constant `C` supports scalar, `[N]`, `[1,N]`, or `[M,N]`; runtime `C` stays on MLA only for `[1,N]`, `[M,1]`, or `[M,N]`."},{"name":"GlobalAveragePool","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"GlobalLpPool","int8":"✅","bf16":"❌","fived":"—","opset":"22","constraint":"Use `p=2` with a static rank-3, rank-4, or rank-5 input. Other values of `p` do not fully lower to MLA."},{"name":"GlobalMaxPool","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"GridSample","int8":"❌","bf16":"✅","fived":"—","opset":"16","constraint":"Set `mode` to `linear`, and do not use `padding_mode=\"reflection\"`. Sampling is supported on the width axis only."},{"name":"HardSigmoid","int8":"✅","bf16":"✅","fived":"—","opset":"6","constraint":""},{"name":"HardSwish","int8":"✅","bf16":"✅","fived":"—","opset":"14","constraint":""},{"name":"InstanceNormalization","int8":"✅","bf16":"✅","fived":"✅","opset":"6","constraint":"Supported for 4D and 5D tensors only."},{"name":"LayerNormalization","int8":"✅","bf16":"✅","fived":"—","opset":"17","constraint":"`stash_type=10` (FLOAT16) is not supported. Leave it at the default."},{"name":"LeakyRelu","int8":"✅","bf16":"✅","fived":"—","opset":"16","constraint":""},{"name":"Log","int8":"✅","bf16":"❌","fived":"—","opset":"13","constraint":""},{"name":"LogSoftmax","int8":"✅","bf16":"❌","fived":"—","opset":"13","constraint":"Input and output must be at least 2D. Any valid static `axis` is handled by automatic layout conversion."},{"name":"LpPool","int8":"✅","bf16":"❌","fived":"—","opset":"22","constraint":"Use `p=2`, `ceil_mode=0`, and dilation 1 with static shapes. Each kernel dimension must be under 128 unless the operation is global pooling."},{"name":"LRN","int8":"✅","bf16":"❌","fived":"—","opset":"13","constraint":""},{"name":"MatMul","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"MaxPool","int8":"✅","bf16":"✅","fived":"—","opset":"12","constraint":"Leave `dilations` at 1, and do not request the optional indices output. Kernel size must be under 128 in each spatial dimension; global pooling has no size limit."},{"name":"Mean","int8":"✅","bf16":"❌","fived":"—","opset":"13","constraint":"All inputs must have the same shape and batch dimension. Use fewer than 128 inputs."},{"name":"MeanVarianceNormalization","int8":"✅","bf16":"❌","fived":"❌","opset":"13","constraint":"Use INT8 activations and rank 4 or lower for portable MLA support. BF16 requests retain INT8 normalization stages, general rank-5 axis layouts are unsupported, and the selected axes must have non-zero variance."},{"name":"Mish","int8":"✅","bf16":"❌","fived":"—","opset":"22","constraint":""},{"name":"Mul","int8":"✅","bf16":"✅","fived":"—","opset":"14","constraint":"Inputs must have the same shape, one input must be a scalar, or the two must be broadcastable."},{"name":"Neg","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"Pad","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"Only `constant` mode with a pad value of 0 is supported, and at most two dimensions may have a non-zero padding width."},{"name":"Pow","int8":"✅","bf16":"✅","fived":"✅","opset":"15","constraint":"The exponent must be a constant scalar of 0.5, -0.5, 2, or 3."},{"name":"PRelu","int8":"✅","bf16":"✅","fived":"—","opset":"16","constraint":"The alpha slope input must be a 1D tensor."},{"name":"Reciprocal","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"ReduceL1","int8":"✅","bf16":"✅","fived":"—","opset":"17","constraint":"Use ONNX opset 17 or earlier. Reducing every spatial axis has no size limit; otherwise each reduced spatial extent must be under 128."},{"name":"ReduceL2","int8":"✅","bf16":"✅","fived":"—","opset":"18","constraint":"Supported only as part of an L2 normalization: it must be followed by a `Div`, so the pair can be fused. A standalone `ReduceL2` is not supported."},{"name":"ReduceLogSum","int8":"✅","bf16":"❌","fived":"—","opset":"17","constraint":"Use ONNX opset 17 or earlier. Reducing every spatial axis has no size limit; otherwise each reduced spatial extent must be under 128. Inputs must produce positive reduced sums."},{"name":"ReduceLogSumExp","int8":"✅","bf16":"❌","fived":"—","opset":"17","constraint":"Use ONNX opset 17 or earlier. Reducing every spatial axis has no size limit; otherwise each reduced spatial extent must be under 128."},{"name":"ReduceMax","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"ReduceMean","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"When reducing over a single axis, that axis must be spatial. Reducing over every spatial axis has no size limit; otherwise each spatial axis must be under 128 elements."},{"name":"ReduceMin","int8":"❌","bf16":"❌","fived":"—","opset":"13","constraint":"Not currently supported."},{"name":"ReduceSum","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"When reducing over a single axis, that axis must be spatial. Reducing over every spatial axis has no size limit; otherwise each spatial axis must be under 128 elements."},{"name":"ReduceSumSquare","int8":"✅","bf16":"✅","fived":"—","opset":"17","constraint":"Use ONNX opset 17 or earlier. Reducing every spatial axis has no size limit; otherwise each reduced spatial extent must be under 128."},{"name":"Relu","int8":"✅","bf16":"✅","fived":"—","opset":"14","constraint":""},{"name":"Reshape","int8":"✅","bf16":"✅","fived":"✅","opset":"14","constraint":"Leave `allowzero` at 0, and give a non-empty target shape."},{"name":"Resize","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"Only `nearest` and `linear` modes are supported, and `coordinate_transformation_mode=\"tf_crop_and_resize\"` is not. With `linear`, any scaling factor works, though in INT8 a factor above 63 may cost accuracy. With `nearest`, the scaling factor must be a power of two, or an even integer applied equally to both dimensions with `coordinate_transformation_mode=\"half_pixel\"` and floor rounding. Only a single input tensor is supported."},{"name":"Sigmoid","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"Sin","int8":"✅","bf16":"❌","fived":"✅","opset":"22","constraint":"Runs as an INT8 lookup-table UDF. In a BF16 model, AFE inserts casts and executes `Sin` in INT8."},{"name":"Slice","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"Steps must be positive. Negative strides are not supported."},{"name":"Softmax","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"Supported over the channel axis only."},{"name":"Softplus","int8":"✅","bf16":"❌","fived":"—","opset":"1","constraint":""},{"name":"Softsign","int8":"✅","bf16":"✅","fived":"—","opset":"22","constraint":""},{"name":"SpaceToDepth","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"Split","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"Sqrt","int8":"✅","bf16":"❌","fived":"—","opset":"13","constraint":""},{"name":"Squeeze","int8":"✅","bf16":"✅","fived":"—","opset":"21","constraint":"The `axes` input must be a compile-time constant, identify only size-1 dimensions, and not remove the batch axis. The output must be at least 2D."},{"name":"Sub","int8":"✅","bf16":"✅","fived":"—","opset":"14","constraint":"Inputs must have the same shape, one input must be a scalar, or the two must be broadcastable."},{"name":"Sum","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"All inputs must have the same rank and batch dimension. Every other dimension must either match or be 1 for broadcasting."},{"name":"Tanh","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":""},{"name":"Tile","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"The `repeats` input must be a compile-time constant, with repeat 1 on the batch axis. Input and output must be at least 2D."},{"name":"TopK","int8":"✅","bf16":"✅","fived":"—","opset":"11","constraint":"`K` must be a compile-time constant between 1 and one less than the selected-axis size, and `largest` must be 1. Input and outputs must be at least 2D. Indices are returned as `int32`, not ONNX `int64`; cast downstream consumers accordingly."},{"name":"Transpose","int8":"✅","bf16":"✅","fived":"—","opset":"13","constraint":"Permutations that move the batch axis are not supported."},{"name":"Unsqueeze","int8":"✅","bf16":"✅","fived":"—","opset":"21","constraint":"The `axes` input must be a compile-time constant and cannot insert a new batch axis. Input and output must be at least 2D."}]
</OperatorTable>

## Constraints

These operators are supported with the limits below. Operators not listed here have no additional constraints. Where a constraint names an ONNX attribute, set it accordingly when you export the model.

- **Add** — Inputs must have the same shape, one input must be a scalar, or the two must be broadcastable.
- **ArgMax** — Reduces along the channel axis only, and returns `int32` rather than the `int64` of the ONNX spec. Cast downstream consumers accordingly.
- **AveragePool** — Set `count_include_pad=True` and leave `dilations` at 1. Kernel size must be under 128 in each spatial dimension; global pooling has no size limit.
- **BatchNormalization** — Export in inference mode. Training mode is not supported.
- **Concat** — Concatenation along the batch axis is not supported.
- **Conv** — `dilations` must be between 1 and 63, and `strides` between 1 and 31.
- **ConvTranspose** — Leave `dilations` at 1. The operator must either be depthwise or use `group=1`. Stride must be 1, 2, 4, 8, or 16, or 1 or 2 when depthwise.
- **Cos** — Runs as an INT8 lookup-table UDF. In a BF16 model, AFE inserts casts and executes `Cos` in INT8.
- **CumSum** — Sums in the forward direction only, along exactly one axis, and that axis must be a constant. The axis can be at most 257 elements long.
- **Div** — Inputs must have the same shape, one input must be a scalar, or the two must be broadcastable.
- **Einsum** — Takes exactly two input tensors. Every index in the equation must appear in at least two of its terms, and no more than once within any single term. For example, `jk,k->k` is unsupported because `j` appears in only one term, and `jk,kk->j` is unsupported because `k` appears twice in the same term.
- **Expand** — The `shape` input must be a compile-time constant and broadcast-compatible with the input. The batch dimension cannot be broadcast or changed.
- **Flatten** — Use `axis=1`. Flattening must preserve the batch dimension, and both input and output must be at least 2D.
- **Gather** — `indices` must be a constant, and either a scalar or a 1D tensor.
- **Gelu** — Only the exact formulation is supported. Set `approximate="none"`.
- **Gemm** — `A` and `B` must be rank 2, and `B` must be a constant initializer. `transA=1` requires flexible batching to be disabled. Constant `C` supports scalar, `[N]`, `[1,N]`, or `[M,N]`; runtime `C` stays on MLA only for `[1,N]`, `[M,1]`, or `[M,N]`.
- **GlobalLpPool** — Use `p=2` with a static rank-3, rank-4, or rank-5 input. Other values of `p` do not fully lower to MLA.
- **GridSample** — Set `mode` to `linear`, and do not use `padding_mode="reflection"`. Sampling is supported on the width axis only.
- **InstanceNormalization** — Supported for 4D and 5D tensors only.
- **LayerNormalization** — `stash_type=10` (FLOAT16) is not supported. Leave it at the default.
- **LogSoftmax** — Input and output must be at least 2D. Any valid static `axis` is handled by automatic layout conversion.
- **LpPool** — Use `p=2`, `ceil_mode=0`, and dilation 1 with static shapes. Each kernel dimension must be under 128 unless the operation is global pooling.
- **MaxPool** — Leave `dilations` at 1, and do not request the optional indices output. Kernel size must be under 128 in each spatial dimension; global pooling has no size limit.
- **Mean** — All inputs must have the same shape and batch dimension. Use fewer than 128 inputs.
- **MeanVarianceNormalization** — Use INT8 activations and rank 4 or lower for portable MLA support. BF16 requests retain INT8 normalization stages, general rank-5 axis layouts are unsupported, and the selected axes must have non-zero variance.
- **Mul** — Inputs must have the same shape, one input must be a scalar, or the two must be broadcastable.
- **Pad** — Only `constant` mode with a pad value of 0 is supported, and at most two dimensions may have a non-zero padding width.
- **Pow** — The exponent must be a constant scalar of 0.5, -0.5, 2, or 3.
- **PRelu** — The alpha slope input must be a 1D tensor.
- **ReduceL1** — Use ONNX opset 17 or earlier. Reducing every spatial axis has no size limit; otherwise each reduced spatial extent must be under 128.
- **ReduceL2** — Supported only as part of an L2 normalization: it must be followed by a `Div`, so the pair can be fused. A standalone `ReduceL2` is not supported.
- **ReduceLogSum** — Use ONNX opset 17 or earlier. Reducing every spatial axis has no size limit; otherwise each reduced spatial extent must be under 128. Inputs must produce positive reduced sums.
- **ReduceLogSumExp** — Use ONNX opset 17 or earlier. Reducing every spatial axis has no size limit; otherwise each reduced spatial extent must be under 128.
- **ReduceMean** — When reducing over a single axis, that axis must be spatial. Reducing over every spatial axis has no size limit; otherwise each spatial axis must be under 128 elements.
- **ReduceMin** — Not currently supported.
- **ReduceSum** — When reducing over a single axis, that axis must be spatial. Reducing over every spatial axis has no size limit; otherwise each spatial axis must be under 128 elements.
- **ReduceSumSquare** — Use ONNX opset 17 or earlier. Reducing every spatial axis has no size limit; otherwise each reduced spatial extent must be under 128.
- **Reshape** — Leave `allowzero` at 0, and give a non-empty target shape.
- **Resize** — Only `nearest` and `linear` modes are supported, and `coordinate_transformation_mode="tf_crop_and_resize"` is not. With `linear`, any scaling factor works, though in INT8 a factor above 63 may cost accuracy. With `nearest`, the scaling factor must be a power of two, or an even integer applied equally to both dimensions with `coordinate_transformation_mode="half_pixel"` and floor rounding. Only a single input tensor is supported.
- **Sin** — Runs as an INT8 lookup-table UDF. In a BF16 model, AFE inserts casts and executes `Sin` in INT8.
- **Slice** — Steps must be positive. Negative strides are not supported.
- **Softmax** — Supported over the channel axis only.
- **Squeeze** — The `axes` input must be a compile-time constant, identify only size-1 dimensions, and not remove the batch axis. The output must be at least 2D.
- **Sub** — Inputs must have the same shape, one input must be a scalar, or the two must be broadcastable.
- **Sum** — All inputs must have the same rank and batch dimension. Every other dimension must either match or be 1 for broadcasting.
- **Tile** — The `repeats` input must be a compile-time constant, with repeat 1 on the batch axis. Input and output must be at least 2D.
- **TopK** — `K` must be a compile-time constant between 1 and one less than the selected-axis size, and `largest` must be 1. Input and outputs must be at least 2D. Indices are returned as `int32`, not ONNX `int64`; cast downstream consumers accordingly.
- **Transpose** — Permutations that move the batch axis are not supported.
- **Unsqueeze** — The `axes` input must be a compile-time constant and cannot insert a new batch axis. Input and output must be at least 2D.
